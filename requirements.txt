# Dependencies for HSI Agent System
# Informational Singularity Hypothesis

# Scientific computing and arrays
numpy>=1.21.0
scipy>=1.7.0

# Machine Learning and clustering
scikit-learn>=1.0.0

# Visualization
matplotlib>=3.5.0
seaborn>=0.11.0

# Data processing
pandas>=1.3.0

# Efficient compression and encoding
bitarray>=2.8.0

# Arbitrary precision math (for Variant J - Pi binary control)
mpmath>=1.3.0

# Optional: Jupyter for interactive analysis
# jupyter>=1.0.0
# ipywidgets>=7.6.0

# Optional: Advanced libraries for fractal analysis
# fractal-dimension>=0.1.0

# ═══════════════════════════════════════════════════════════════════
# LEVEL 2: Advanced Analysis (Optional but recommended)
# ═══════════════════════════════════════════════════════════════════

# Topological Data Analysis (TDA)
ripser>=0.6.0             # Fast persistent homology (lightweight, ~5MB)
# giotto-tda>=0.6.0       # Full TDA suite (heavy ~500MB, use if need more features)

# Transfer Entropy (optional, we have built-in histogram implementation)
# pyinform>=0.2.0         # Only if you need advanced TE methods

# JIT Compilation for performance
numba>=0.58.0

# System monitoring for adaptive parallelism
psutil>=5.9.0

# Progress bars
tqdm>=4.65.0

# Optional: GPU acceleration (NVIDIA CUDA)
# cupy-cuda11x>=9.0.0  # For NVIDIA CUDA 11.x
