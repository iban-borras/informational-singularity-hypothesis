# D-0154 Downstream Re-derivation Matrix

- Date: 2026-07-03
- Status: `draft-operational-matrix`
- Scope: downstream paper-facing re-derivations after clean Level 0 regeneration for affected variants `{E, I, D, G, F}`
- Parent decision: `HSI_v2_Decision_Log.md` D-0154
- Policy closure audit: D-0154 Amendment 3, Grace `APPROVED AND CLOSED`, `docs/HSI_v2_Audit_D0154_Level0_Sanitation_Closure.md`, SHA-256 `DCBFDAB2E7F858F72022C1B511E1967EBAA2ED973B87169DD16793D08897B275`

## Purpose

D-0154 Level 0 sanitation is materially and methodologically closed: clean post-patch sources exist for the affected paper-facing variants, and Grace has approved the integrity of the sanitation closure. This does **not** revalidate manuscript values by itself. Every paper-facing table, figure, or prose claim that depends on pre-patch `{E, I, D, G, F}` data must be re-derived from clean snapshots or explicitly marked as not numerically dependent.

This matrix sequences the downstream work. It is an operational planning artifact, not an execution contract. Any expensive or claim-changing re-derivation still needs the usual manifest, hashes, and Decision Log amendment.

The canonical Priority A list is the D-0154 policy item 3 list. This matrix does not supersede it and does not authorize any reordering of scientific priority. Any proposed reordering relative to D-0154 must be argued explicitly and accepted by Iban/Sofia in a future amendment.

## Clean Source Anchors

| Variant | Clean depth | Source status | Closure reference |
|---|---:|---|---|
| `E` | `@24` | Clean via Q6-A Stage 0; downstream re-derivation only | D-0152 Amendment 2 |
| `I` | `@23` | Clean Level 0 regeneration closed | D-0154 Amendment 1 |
| `D` | `@20` | Clean Level 0 regeneration closed | D-0154 Amendment 2 |
| `G` | `@20` | Clean Level 0 regeneration closed | D-0154 Amendment 2 |
| `F` | `@20` | Clean Level 0 regeneration closed | D-0154 Amendment 2 |

Unaffected or out-of-scope variants for this matrix: `B` (unaffected code path), `H@20` (below affected threshold per D-0153), `N` (clean under paper-facing regime), `A/J/K/L/M/O/P` (not generated by the affected Level 0 branch or already externally controlled).

## Priority Rules

- **Priority A**: paper-facing tables/figures explicitly listed in D-0154 policy item 3, plus direct visual artifacts derived from those tables. These must close before manuscript reconciliation. This is the canonical list unless a future D-0154 amendment says otherwise.
- **Priority A-check**: paper-facing prose or interpretive tables that may change if Priority A values move, but do not require a distinct heavy run.
- **Priority B**: internal Phase 2/3/4 diagnostics or appendix figures that use affected variants in B/E or E/B contrasts. These do not block the first Priority A table reconciliation unless Sofia/Grace explicitly promote them.
- **Hold**: do not edit the manuscript from any row until the row has a re-derivation report or a no-change equivalence report.

## Matrix

| Affected artifact | Variant dependency | Re-derive? | Priority | Suggested source/protocol | Output needed |
|---|---|---:|---|---|---|
| `tab:signatura` plus §6.1/§6.2 numeric prose | `E`, `I`, `F`; prose also names `D`, `G` | Yes | A | Recompute signature metrics used in the table/prose from clean snapshots: LZ, `c_bar`, TE ratio, fractal dimension if present, DET where present. Preserve existing metric definitions; no new thresholds. | `summary.json`, `report.md`, per-metric CSV, manifest with clean source hashes. |
| `tab:pm_size` | `E`, `I`, `F`; `B/E` combined row depends on clean `E` consistency | Yes | A | Canonical Phase 1 tower run over affected variants on the manuscript ladder `m={8,12,16,20,24,28,32}` with `3x1M`, prefix/suffix, canonical stability thresholds. | `phase1_summary.json`, `dataset.json`, manifest, row-level comparison vs previous values. |
| `tab:fibres` | `E`, `I`, `F`; `B/E` combined row depends on clean `E` consistency | Yes | A | Same canonical Phase 1 run as `tab:pm_size`; derive prefix fiber diagnostics at `12->8` and `32->28`. | Fiber rows for affected variants, manifest, comparison table. |
| prefix/suffix asymmetry paragraph/table in §7.3 | `E`, `I`, `F`; `B/E` combined row depends on clean `E` consistency | Yes | A | Same canonical Phase 1 run; derive prefix/suffix child-entropy gaps at `32->28`. | Asymmetry row CSV/report; note if row signs or magnitudes change. |
| `tab:transport` | `E`, `I`, `F`; `B/E` combined row depends on clean `E` consistency | Yes | A | Canonical `phase1-transport` path over the same Phase 1 runs, main policy `prefix`, control policy `suffix`, `tail_edges=2`. | Transport row CSV/report with breadth ratio, `H_trans`, `mu_act`, signed/relative gaps. |
| `tab:porta1_canonica` | `E`, `I`, `F`; null rows `E-matched-lz`, `I-matched-lz` may need clean-source regeneration | Yes | A | Canonical gate-plane derivation from Phase 1 summaries and transport rows. If nulls are source-derived from `E` or `I`, regenerate from clean sources under the exact original null protocol before reading. | Gate-plane CSV/report with `retention@last`, `surv_last`, `active_mean_tail`; manifest with source and null hashes. |
| `tab:hsi_family_closure` | `D`, `G`; `H` is clean but included as comparator; `F` may affect nearby prose/category contrast | Yes | A | Re-run `phase1_hsi_family_closure` equivalent for clean `D@20`, `G@20`, and clean comparator `H@20`; optionally include clean `F@20` only if Sofia wants one consolidated family table. | Full table CSV/report, manifest, explicit old-vs-clean diff. |
| `fig:projective_tower_survival` | `E`, `I`, `F`; visual support for `tab:pm_size` | Yes, if any table row changes; otherwise no-change rebuild preferred | A | Re-run `hsi_v2_phase1_projective_tower_survival_figure.py` from clean Phase 1 summaries after `tab:pm_size` closes. | PDF/PNG/data JSON/manifest; visual diff note. |
| `fig:gate_plane` (`phase1_gate_plane_tail2.png`) | `E`, `I`, `F`; also `E/I` source-derived nulls if regenerated | Yes, after `tab:porta1_canonica` closes | A | Re-run the gate-plane figure path from the cleaned gate-plane CSV. Preserve O/P handling and label offsets. | PNG/SVG/CSV/JSON/manifest; paper PNG only after audit. |
| `tab:taxonomia` / interpretive synthesis of regimes | `E`, `I`, `F`, `D`, `G` indirectly | Check after A rows | A-check | No independent heavy run; compare cleaned Phase 1/tower/transport outcomes against current class assignments. | Short decision note: unchanged / wording update needed. |
| §6 robustness prose: B/E/I commutativity and B-vs-F gradient | `E`, `I`, `F` | Check after signature + Phase 1 | A-check | Use cleaned signature and Phase 1 outputs. | Editorial note listing any changed numeric values or unchanged claims. |
| §9.10 HSI-family closure prose | `D`, `G`, `H`; indirectly `F` as rigid comparator | Check after `tab:hsi_family_closure` | A-check | Use cleaned family-closure report. | Editorial note: table unchanged/changed; any class wording affected. |
| `fig:phase2_deficit_jitter` | `E` anchor, `B` candidate, matched-LZ controls | Re-derive or explicitly validate | B-high | Grace specifically flagged parent-density / parent-shell atlas family. Re-run or validate Phase 2 parent-shell atlas sensitivity inputs from clean `E` before paper-facing reuse. | Phase 2 report/manifest, figure artifact hashes if regenerated. |
| `fig:phase2_lag_response` | `E` anchor, `B` candidate, matched-LZ controls | Re-derive or explicitly validate | B-high | Re-run or validate N2-11 child-routing lag-response over clean `E` anchor. Existing figure is paper-facing appendix support. | Lag-response report/manifest, figure artifact hashes if regenerated. |
| `tab:phase3_robustness_primary` | `B/E` flow scores and LZ-family null envelopes | Re-derive or explicitly validate after Phase 2 inputs | B | Phase 3 robustness matrix depends on the Phase 2/3 component artifacts. Do not re-narrate until upstream clean E-dependent scaffold is validated. | Robustness matrix report/manifest; no manuscript edit without audit. |
| `tab:phase3_channel_matrix` | `B/E` flow scores and LZ-family null envelopes | Re-derive or explicitly validate after Phase 2 inputs | B | Same dependency chain as `tab:phase3_robustness_primary`. | Channel matrix report/manifest; no manuscript edit without audit. |
| Q6-A Stage 1 high-scale symmetric tower | `B@24`, `E@24` | No immediate re-derive | Done / reference | Already executed from clean `E@24` generated after the patch; not contaminated by pre-patch E. | Keep as reference; no action unless Sofia requests integration with Priority A. |
| Q6-A Stage 3 P4@24 symmetric forecast | `B@24`, `E@24` | No immediate re-derive | Done / reference | Already executed from clean `E@24`; contractual outcome was `inputs-or-run-not-derivable`. | No manuscript use under current freeze. |
| Post-review Item 2 (`B-markov1@24`) | `B` only | No D-0154 dependency | Independent | Contract D-0155 v1.0 frozen. B was not affected by the Level 0 bug. | Can execute when compute resources are free; does not close D-0154 paper-facing revalidation. |
| Future Item 3 (`B` vs `B_XOR`) | `B`, `B_XOR`; may use controls for context | Re-check design if controls include affected variants | Future | Keep design principle: compare what `B_XOR` preserves vs loses, not make `B` win. | New prereg if executed. |
| Future Item 4 permutation/statistical reinforcement | likely envelope over current cleaned variants/nulls | Wait | Future after A | Should use cleaned re-derived artifacts, not pre-patch tables. | Separate contract. |

## Recommended Execution Order

1. **Priority A Phase 1 batch**: derive clean Phase 1 summaries for affected variants needed by `tab:pm_size`, `tab:fibres`, asymmetry, `tab:transport`, and `tab:porta1_canonica`. This is an execution sequence inside canonical Priority A, not a demotion of `tab:signatura`.
2. **Priority A signature batch**: recompute `tab:signatura` metrics/prose dependencies for `E/I/F/D/G` as applicable.
3. **Priority A family closure batch**: re-derive `tab:hsi_family_closure` for `D/G` with clean source anchors and validate `H` as comparator.
4. **Visual refresh**: rebuild `fig:projective_tower_survival` and `fig:gate_plane` only after their numeric tables close.
5. **A-check editorial reconciliation**: Sofia decides which prose/table cells change; no manuscript patch before all Priority A rows are closed and audited.
6. **Priority B diagnostics**: revalidate Phase 2/3 appendix artifacts if they remain paper-facing after Priority A reconciliation.
7. **Independent Item 2**: may run when compute is free because it is `B`-only; keep it separate from D-0154 amendments.

## Stop / Escalation Conditions

- If any clean re-derived value changes a paper-facing classification (`HSI-like`, `rigid`, `crystalline`, `collapse`, `diffuse Markov`, `matched-LZ-like`), stop manuscript reconciliation and request Sofia/Grace review before editing.
- If clean `E` no longer matches the `B/E` combined rows within the existing row semantics, split `B/E` into separate rows rather than averaging or silently preserving the combined label.
- If a source-derived null (`E-matched-lz`, `I-matched-lz`) cannot be regenerated under the original protocol, mark the affected row `inputs-not-derivable`; do not substitute `B` nulls.
- If a Phase 2/3 artifact depends on a pre-patch E anchor and cannot be re-derived at reasonable cost, it must be downgraded to repo-facing only or explicitly frozen pending a new contract.
